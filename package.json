{
  "scripts": {
    "lint": "prettier -w .",
    "check": "prettier -c .",
    "test": "TEST=true node index.mjs"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.470.0",
    "@aws-sdk/client-s3": "^3.470.0",
    "express": "^4.18.2",
    "prettier": "^3.1.1",
    "serverless-http": "^3.2.0"
  }
}
